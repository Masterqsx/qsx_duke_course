#include "point.h"
class Circle
{
  private:
	Point center;
	const double radius;

  public:
	Circle(Point p, double ra);
	void move( double dx, double dy);
	double intersectionArea(const Circle & otherCircle);
	double intersectionTest(const Circle & otherCircle,int n);
};
